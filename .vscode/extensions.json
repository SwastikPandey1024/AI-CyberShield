{
    "recommendations": [
        // ──────────────────────────────────────────────
        // Python Development
        // ──────────────────────────────────────────────
        "ms-python.python", // Python language support (IntelliSense, debugging, environments)
        "ms-python.vscode-pylance", // Fast, feature-rich Python type checking
        "ms-python.black-formatter", // Black code formatter integration
        "ms-python.ruff", // Ruff linter integration
        "ms-python.isort", // Import sorting
        "ms-python.mypy-type-checker", // Static type checking
        // ──────────────────────────────────────────────
        // Docker & Containers
        // ──────────────────────────────────────────────
        "ms-azuretools.vscode-docker", // Dockerfile and docker-compose support
        "ms-vscode-remote.remote-containers", // Dev container support
        // ──────────────────────────────────────────────
        // GitHub & Version Control
        // ──────────────────────────────────────────────
        "github.vscode-github-actions", // GitHub Actions workflow visualization
        "eamodio.gitlens", // Git blame, history, and code lens
        "mhutchie.git-graph", // Interactive git commit graph
        // ──────────────────────────────────────────────
        // Markdown & Documentation
        // ──────────────────────────────────────────────
        "yzhang.markdown-all-in-one", // Markdown editing (table of contents, preview, lists)
        "davidanson.vscode-markdownlint", // Markdown linting and style checking
        // ──────────────────────────────────────────────
        // Jupyter & Notebooks
        // ──────────────────────────────────────────────
        "ms-toolsai.jupyter", // Jupyter notebook support
        "ms-toolsai.jupyter-keymap", // Jupyter key bindings
        "ms-toolsai.vscode-jupyter-cell-tags", // Jupyter cell tagging
        "ms-toolsai.jupyter-renderers", // Jupyter output renderers (plots, dataframes)
        // ──────────────────────────────────────────────
        // Data & ML
        // ──────────────────────────────────────────────
        "tamasfe.even-better-toml", // TOML file support (pyproject.toml)
        "mechatroner.rainbow-csv", // CSV file colorization for dataset inspection
        // ──────────────────────────────────────────────
        // General Productivity
        // ──────────────────────────────────────────────
        "streetsidesoftware.code-spell-checker", // Spell checker for code and comments
        "redhat.vscode-yaml" // YAML validation and schema support (docker-compose, CI/CD)
    ]
}
